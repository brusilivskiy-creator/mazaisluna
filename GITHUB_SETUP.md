# 🚀 Налаштування для GitHub

## Крок 1: Додавання змінних оточення в GitHub

Для того, щоб база даних працювала на GitHub/Vercel/Netlify, потрібно додати секретні змінні:

### Для GitHub Actions:

1. Перейдіть у ваш репозиторій на GitHub
2. Натисніть **Settings** → **Secrets and variables** → **Actions**
3. Натисніть **New repository secret**
4. Додайте наступні змінні:

   - **Name**: `DATABASE_URL`
   - **Value**: Ваш connection string з Prisma Remote
     ```
     postgres://user:password@db.prisma.io:5432/postgres?sslmode=require
     ```

   - **Name**: `NEXT_PUBLIC_SITE_URL` (опціонально)
   - **Value**: URL вашого сайту (наприклад: `https://your-site.netlify.app`)

   - **Name**: `NEXT_PUBLIC_API_URL` (опціонально)
   - **Value**: URL вашого API (зазвичай такий самий як SITE_URL)

### Для Vercel:

1. Перейдіть у ваш проєкт на [Vercel](https://vercel.com)
2. Натисніть **Settings** → **Environment Variables**
3. Додайте `DATABASE_URL` з вашим connection string

### Для Netlify:

1. Перейдіть у ваш проєкт на [Netlify](https://www.netlify.com)
2. Натисніть **Site settings** → **Environment variables**
3. Додайте `DATABASE_URL` з вашим connection string

## Крок 2: Оновлення build команди

Переконайтеся, що build команда включає генерацію Prisma Client:

```json
{
  "scripts": {
    "build": "prisma generate && next build"
  }
}
```

Або в налаштуваннях хостингу додайте:
- **Build command**: `npm run db:generate && npm run build`

## Крок 3: Перевірка .gitignore

Переконайтеся, що `.env` файл ігнорується:

```
.env
.env.local
.env*.local
```

## Крок 4: Коміт та пуш

```bash
git add .
git commit -m "Add Prisma database setup"
git push origin main
```

## Перевірка

Після деплою перевірте, що:
- ✅ База даних підключається
- ✅ API routes працюють
- ✅ Дані відображаються на сайті

Якщо виникають проблеми, перевірте логи деплою та переконайтеся, що `DATABASE_URL` правильно встановлений.

