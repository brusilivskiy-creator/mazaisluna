# 🚀 Деплой на Netlify - Швидка інструкція

## Крок 1: Підготовка репозиторію на GitHub

1. Переконайтеся, що всі зміни закомічені:
   ```bash
   git status
   git add .
   git commit -m "Ready for deployment"
   ```

2. Якщо репозиторій ще не створено, створіть на GitHub та зробіть push:
   ```bash
   git remote add origin https://github.com/your-username/your-repo.git
   git branch -M main
   git push -u origin main
   ```

## Крок 2: Деплой на Netlify через GitHub

1. Перейдіть на [Netlify](https://www.netlify.com/) та увійдіть/зареєструйтесь

2. Натисніть **"Add new site"** → **"Import an existing project"**

3. Оберіть **"GitHub"** та авторизуйтесь

4. Оберіть ваш репозиторій

5. Netlify автоматично визначить налаштування з `netlify.toml`:
   - **Build command**: `npm run build`
   - **Publish directory**: `.next`
   - **Node version**: 20

6. Натисніть **"Deploy site"**

## Крок 3: ⚠️ ВАЖЛИВО - Налаштування змінних оточення

Після першого деплою, **ОБОВ'ЯЗКОВО** додайте змінні оточення:

1. В панелі Netlify перейдіть до вашого сайту
2. Перейдіть в **Site settings** → **Environment variables**
3. Додайте змінну:
   - **Key**: `DATABASE_URL`
   - **Value**: Ваш connection string з Prisma Remote
     ```
     postgres://user:password@host:5432/database?sslmode=require
     ```
   - **Scopes**: Production, Preview, Deploy previews (виберіть всі)
4. Натисніть **Save**

### Додаткові змінні (опціонально):

- `NEXT_PUBLIC_SITE_URL` - URL вашого сайту (наприклад: `https://your-site.netlify.app`)
- `NEXT_PUBLIC_API_URL` - URL API (якщо потрібен)

5. Після додавання змінних перейдіть в **Deploys** → **Trigger deploy** → **Clear cache and deploy site**

## Крок 4: Перевірка після деплою

1. Перевірте логи збірки - мають містити:
   ```
   ✔ Generated Prisma Client
   ✔ Build completed successfully
   ```

2. Перевірте роботу сайту:
   - Головна сторінка
   - Адмін-панель: `https://your-site.netlify.app/admin`
   - API routes: `https://your-site.netlify.app/api/politicians`

3. Перевірте базу даних - переконайтеся, що дані завантажуються

## Автоматичний деплой

Після підключення GitHub, кожен push до `main` гілки автоматично запускає новий деплой на Netlify.

## 🔧 Альтернатива: Деплой через Netlify CLI

Якщо ви хочете деплоїти через командний рядок:

```bash
# Встановіть Netlify CLI (якщо ще не встановлено)
npm install -g netlify-cli

# Ввійдіть в Netlify
netlify login

# Ініціалізуйте проєкт (перший раз)
netlify init
# Виберіть:
# - Create & configure a new site
# - Team: ваш team
# - Site name: введіть назву або натисніть Enter для автоматичної

# Деплой в production
netlify deploy --prod

# Або деплой preview (для тестування)
netlify deploy
```

## 🐛 Troubleshooting

### Помилка: "Prisma Client not generated"

**Рішення**: Переконайтеся, що build команда включає `prisma generate` (вже є в `package.json`):
```json
"build": "prisma generate && next build"
```

### Помилка: "Can't reach database server"

**Рішення**: 
- Перевірте, що `DATABASE_URL` правильно встановлений в Environment Variables
- Перевірте, що база даних активна в Prisma Console
- Переконайтеся, що connection string правильний

### Помилка: "Build failed"

**Рішення**:
- Перевірте логи збірки в Netlify Dashboard
- Локально запустіть `npm run build` для перевірки помилок
- Переконайтеся, що всі залежності встановлені

### Помилка: "Environment variable not found"

**Рішення**:
- Переконайтеся, що змінна додана в Site settings → Environment variables
- Перевірте назву змінної (має бути `DATABASE_URL`)
- Після додавання змінної обов'язково перезапустіть деплой

## 📝 Примітки

- Файл `netlify.toml` вже налаштований і включений в репозиторій
- Build команда автоматично генерує Prisma Client
- Node версія встановлена на 20 (в `netlify.toml`)
- Плагин Next.js автоматично встановлюється під час деплою

## 🔗 Корисні посилання

- [Netlify Documentation](https://docs.netlify.com/)
- [Next.js on Netlify](https://docs.netlify.com/integrations/frameworks/next-js/)
- [Prisma Deployment Guide](https://www.prisma.io/docs/guides/deployment)

