# 🚀 Быстрый старт - Настройка базы данных

## Шаг 1: Создайте базу данных в Prisma Remote

1. Перейдите на https://console.prisma.io
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите "Create Database"
4. Заполните форму:
   - **Name**: `mazais`
   - **Region**: `eu-central-1` (или ближайший к вам)
   - **Plan**: выберите подходящий план
5. После создания, скопируйте **Connection String** (Prisma format)

## Шаг 2: Настройте .env файл

Создайте файл `.env` в корне проекта и добавьте:

```env
DATABASE_URL="ваш-connection-string-здесь"
```

## Шаг 3: Выполните команды

После настройки `.env`, выполните:

```bash
# Применить схему к базе данных
npm run db:push

# Заполнить базу данных данными
npm run db:seed
```

## Альтернатива: Используйте скрипт автоматизации

Выполните PowerShell скрипт:

```powershell
.\setup-database.ps1
```

Он проведет вас через все шаги автоматически.

## Проверка

После выполнения всех шагов, откройте Prisma Studio для просмотра данных:

```bash
npm run db:studio
```

База данных будет доступна на http://localhost:5555

